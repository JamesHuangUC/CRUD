<div class="row">
    {{#each gigs}}
        <div class="col-md-4">
            <div class="card">
                <img src="{{cover}}" class="card-img-top img-thumbnail" alt="...">
                <div class="card-body">
                    <h5 class="card-title">{{title}}</h5>
                    <p class="card-text text-truncate">{{description}}</p>
                    <p class="card-text">{{budget}}</p>
                    <a href="/gigs/{{id}}" class="stretched-link"></a>
                </div>
                <div class="card-footer">
                    <small class="text-muted">{{technologies}}</small>
                </div>
            </div>
        </div>
    {{else}}
        <p>No gigs available</p>
    {{/each}}
</div>

{{#if pages}}
<ul class="pagination text-center">
    {{#ifequal this.current 1}}
        <li class="page-item disabled"><a class="page-link">First</a></li>
    {{else}}
        <li class="page-item"><a class="page-link" href="/gigs/pages/1">First</a></li>
    {{/ifequal}}

    {{{pagebar this.current this.pages}}}

    {{#ifequal this.current this.pages}}
        <li class="page-item disabled"><a class="page-link">Last</a></li>
    {{else}}
        <li class="page-item"><a class="page-link" href="/gigs/pages/{{pages}}">Last</a></li>
    {{/ifequal}}
</ul>
{{/if}}
